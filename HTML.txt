LINK, Curso HTML y CSS --- Dalto:
https://www.youtube.com/watch?v=ELSm-G201Ls

minu: 2:08:25


TÍTULOS Y SUB:
h1, h2, h3, h4, h5, h6.

LISTAS:
---> Ordenadas: <ol> / Para sacar el númerito de adelante en CSS: list-style: none;
---> items: li
--* Desordenadas: <ul> 

ENLACES:
<a href="(aquí va la dirección)"> /si quiero poner un enlace externo, uso: https:// ó //, no es muy recomendado lo último.
si quiero que el vinculo se abra en otra pestaña: target="_blank"

FORMULARIO:       
<form>
input type = "text" required (obliga al usuario que escriba algo en este campo)
input type = "email",
input type = "password",
input type = "checkbox",
input type = "radio",
input type = "submit",
input type = "file",
input type = "date",
        
---ATRIBUTOS DEL FORM
- required (obliga al usuario que escriba algo en este campo). **Conf de funcionalidad**
- name= (identificador de los inputs) -***Confg interna***
- placeholder = (Escribe un pequeño texto que desaparace cuando el usuario empieza a escribir)
- value (Lo que va a recibir el servidor).